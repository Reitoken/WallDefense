# WallDefense — Project Instructions

## Repository layout
- `WallDefense/` — the Unreal Engine 5.7 C++ project (code in `Source/`, assets in `Content/`).
- `Production/` — everything game-related outside the engine: game design, art sources, audio, documents, references.

## Documentation language rule (Production/ only)
Pierre is the only developer on the project (solo since June 2026). All Markdown documents in `Production/` are written in **French only** — no `_en`/`_fr` duplicates. Technical files elsewhere (CLAUDE.md, code comments, .vscode, etc.) stay in English.

## Build
- Engine: `C:\Unreal Engine\UE_5.7`
- Build editor target: `& "C:\Unreal Engine\UE_5.7\Engine\Build\BatchFiles\Build.bat" WallDefenseEditor Win64 Development -project="c:\GitHub\WallDefense\WallDefense\WallDefense.uproject" -waitmutex` (VS Code: `Ctrl+Shift+B`)
- External builds of the Editor target FAIL while the Unreal editor is open (Live Coding). Ask to close the editor or use Ctrl+Alt+F11 inside it.

## Key documents (read before designing or coding)
- `Production/01_GameDesign/GDD_WallDefense.md` — THE game design reference (update it when adding mechanics).
- `Production/04_Documents/Technical/ArchitectureTechnique.md` — THE technical reference: classes, components, DataAssets, UI binding, socket conventions, 7-step implementation plan (§12).
- `Production/04_Documents/Technical/Avancement.html` — presentable progress report (French, self-contained HTML). UPDATE IT at every delivered step: roadmap status, implemented classes, "how to test in Unreal" guide, automated test results.
- Pierre gives feedback by writing `## BAK:` comments inside documents — read them, integrate them, then remove them.

## Implementation status (update this section as steps complete)
- **Step 1/7 DONE** (foundations): `Core/WDTypes.h` (EWDElement, FWDDamageEvent, FWDElementalProfile, element colors), `Combat/WDHealthComponent` (elemental weakness ×1.75 / resistance ×0.5, percent defense, shield layer, events), `Core/WDDebugSubsystem` (element-colored debug draw, CVar `wd.Debug.Draw`).
- **Step 2/7 DONE** (top-down hero): `Player/WDHeroMath.h` (input→world math, screen up = world +X), `Player/WDHeroCharacter` (twin-stick: aim-driven rotation, Zelda-like camera onboard, anim state getters), `Player/WDHeroController` (Enhanced Input built at runtime in C++ — WASD+ZQSD/mouse aim via ground-plane intersection, gamepad sticks; placeholder fire/switch draw debug), `Player/WDHeroAnimInstance`, `GameModes/WDSandboxGameMode` (test: GameMode Override in any map with a floor).
- **Step 3/7 DONE** (weapons debug-first): `Weapons/WDWeaponData` (DataAsset, 4 fire modes, `MakeDebugArsenal()` = the 7 level-1 weapons when no assets assigned), `Weapons/WDWeaponMath.h` (reflect/spread/ring/homing — unit tested), `Weapons/WDProjectile` + `WDProjectilePoolSubsystem` (generic data-driven projectile: pierce, bounce, fragments, chain, homing; manual sphere sweeps, no collision component), `Weapons/WDWeaponComponent` (projectile / continuous cone / bouncing ray / targeted strike) + `WDWeaponInventoryComponent` (slots + the single switch), `Weapons/WDTargeting` (damageable scans), `Combat/WDTargetDummy` (training target: tint = weakness color, FAIBLESSE/résisté debug numbers, respawn). Sandbox auto-spawns PlayerStart + 3 dummies. Hero has a visible debug body (engine Cylinder — note: UE 5.7 has NO Capsule basic shape).
- **Step 4/7 DONE** (monsters + waves + hit response): `Monsters/WDMonsterData` (+`MakeZone1Bestiary()`: Ombrillon/Vif-Esprit/Rempart/Crache-Brume/Vestale + boss Brute dimensionnelle), `WDPatternMath.h` + `WDMovePatternComponent` (8 patterns), `WDWallAttackComponent` (melee/ranged via pool, `FWDShotParams.IgnoredClass` = friendly-fire filter), `WDAuraHealComponent`, `WDMonster` (AActor assembling components, role-shaped debug body tinted by weakness), `WDMonsterAnimInstance`, `Stage/WDStageData` (+`MakeDebugStage1()`: 5 waves, boss last) + `AWDStageDirector` (inter-wave timer with EARLY start when cleared), `Combat/WDHitResponseComponent` (flash/knockback/damage numbers) + `Core/WDGameFeelSubsystem` (hitstop via time dilation, gamepad rumble). Sandbox spawns a wall stand-in (wide dummy 2000HP) + director, stage auto-starts after 5 s.
- **Step 5/7 DONE** (the wall + the loop): `Wall/WDWallData` (DA_Wall: HP/defense per level, one-shot skills + `MakeDebugWall()`), `WDWallSkillsComponent` (one-shot conditional skills: starting shield / repulsion wave on first contact / auto-repair under 25% — pure decision logic, owner applies effects), `WDWall` (wide façade, reddens with damage), `Stage/WDStageMath.h` (stars: 100%=3 / ≥66%=2 / ≥33%=1, reward ×1/×1.25/×1.5/×2), `GameModes/WDStageGameMode` (THE loop assembler: hero+wall+director, victory/defeat, debug auto-restart via OpenLevel; sandbox is back to training-range only), `Core/WDPreloadSubsystem` (GameInstance subsystem: collects ALL TSoftObjectPtr by reflection from stage DataAssets, async load, pool warmup, progress/finished delegates), `UI/WDLoadingScreenWidget` (event-driven, builds its own debug tree in C++ when no BP design; Build.cs now has UMG/Slate). Monsters now aim at the closest point of the target's collision (spread along the façade) and `WDMonster::PushBack()` serves the repulsion wave.
- **Step 6/7 DONE** (meta): `Loot/WDLootPickup` (10s lifetime + blink, no collision) + `WDLootDropComponent` (gold/XP/weakness-element resource, deterministic `BuildDrops` with FRandomStream, drop tables on `WDMonsterData`), `Player/WDMagnetComponent` (pull+collect, radius grows with character level), `Core/WDProgressionMath.h` (debug curves: XP, magnet, costs, tiers ≤40/≤70/100, ceil multiplier) + `WDProgressionSubsystem` (gold/resources(element×tier)/XP/weapon levels/wall level/stars-per-stage×mode; `ApplyRunRewards` returns the granted bundle; failed purchases spend nothing) + `WDSaveGame`/`WDSaveSubsystem` (5 slots `WDSlot_0..4`, auto-load slot 0 at Initialize, auto-save after stages/purchases) + `WDSettingsSubsystem` (language/volumes/quality in user config; first-launch `RunHardwareBenchmark`, skipped when `FApp::IsUnattended`), `UI/WDWidgetKit.h` + `WDHubWidget`/`WDPauseWidget`/`WDStageSummaryWidget` (code-built trees; runtime-chosen button handlers bind via `FScriptDelegate::BindUFunction`, NOT AddDynamic — it stringifies), `GameModes/WDHubGameMode`. Same-map navigation via `OpenLevel(..., "game=/Script/WallDefense.WD*GameMode")`. Weapon meta level scales damage in `WDWeaponComponent::SetWeapon` (+8%/level debug). Pause key: P (Escape kills PIE), Start on gamepad; abandon = defeat, loot kept ×1.
- 37 automated tests passing (`Health`, `Hero`, `Weapons`, `Patterns`, `Monsters`, `Stage`, `Wall`, `Preload`, `Loot`, `Progression`, `Save`). Note: GameInstance subsystems in tests need a `NewObject<UGameInstance>()` outer (ClassWithin).
- **Next: Step 7** — the rest: Specials + videos, weapon milestones 1-100, Hard/Hell modes, encyclopedia, outfits, story, dynamic music, Steam, input remapping, stages 2+ / other zones.
- Legacy lane-based code (`LaneGrid`, `LanePlayerCharacter`, old `HealthComponent`, `BaseMonster`) is still in place; it gets replaced/migrated as steps 2–4 land.

## Code conventions
- New classes use the `WD` prefix (`UWDHealthComponent`, `AWDHeroCharacter`...). Code identifiers in English; UI text via `FText` only.
- Debug-first: gameplay must be testable without any art assets (debug draw through `UWDDebugSubsystem`).
- Components are independent (no component includes another component's header); communication via delegates; data via DataAssets (`TSoftObjectPtr`). See ArchitectureTechnique §1 and §10.
- Every step ships with automation tests. Run headless:
  `& "C:\Unreal Engine\UE_5.7\Engine\Binaries\Win64\UnrealEditor-Cmd.exe" "c:\GitHub\WallDefense\WallDefense\WallDefense.uproject" -ExecCmds="Automation RunTests WallDefense; Quit" -nullrhi -unattended -nopause`
- Asset naming: see `WallDefense/ReadeMe.txt` (BP_, SM_, M_, T_, WBP_, DA_, DT_...).
